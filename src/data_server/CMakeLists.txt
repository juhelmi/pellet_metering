set(LIBRARY_SOURCES
    #"measurement_server.cc"
    )
set(LIBRARY_HEADERS
    #"measurement_server.h"
    )

include_directories(
  )

# Pointer to json include also
include_directories(  "${CMAKE_SOURCE_DIR}/external/json/include")

